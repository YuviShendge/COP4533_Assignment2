
**Name:** Yuvika Shendge  
**UFID:** 61747967

**Compilation and Build Instructions:**
From the root directory:

```
To reproduce the example test:

python src/cacheEviction.py data/example.in

ompare the result to `data/example.out`.
```

**Repository Structure:**
```
src/
    cacheEviction.py

data/
    example.in
    example.out
    example2.in

README 
```

**Assumptions:**

Input format is:

```
k m
r1 r2 r3 ... rm
```

All request IDs are integers.
 The program assumes valid input format.
 The program does not perform additional input validation beyond parsing.

